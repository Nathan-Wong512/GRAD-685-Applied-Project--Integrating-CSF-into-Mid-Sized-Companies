Small, repeatable ops loop mapped to NIST CSF:
- **Identity:** MFA% + stale-account exceptions (`users.csv` → `ia_findings.csv`)
- **Endpoints:** baseline → drift → remediate → log (`compliance.csv`)
- **Data/DR:** restore drill with **RPO**, **RTO**, and **SHA-256** integrity
